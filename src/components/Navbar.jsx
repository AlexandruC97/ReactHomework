import React from "react";

function Navbar() {
    return (<div id = "navbar" >
            <a className="navbarItem" href="#">TRAVEL UPDATES</a>
            <a className="navbarItem" href="#">REVIEWS</a>
            <a className="navbarItem" href="#">ABOUT</a>
            <a className="navbarItem" href="#">CONTACT</a>
        </div>
    )
}

export default Navbar;